# Frontend (Vue 3 + Vite)

This is a minimal Vue 3 app that calls the backend endpoint /api/health (proxied to http://localhost:8080 by Vite during development).

Run:

```bash
cd frontend
npm install
npm run dev
```

Open http://localhost:5173
